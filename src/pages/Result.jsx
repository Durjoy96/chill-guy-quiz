import { useContext } from "react";
import { contextProvider } from "../provider/Provider";

const Result = () => {
  const { points } = useContext(contextProvider);

  return <div>

        

  </div>;
};

export default Result;
